const NailPolish = ["Black", "Royal Blue"];

// will return one nail polish randomly from the array
const randomElement = NailPolish[Math.floor(Math.random() * NailPolish.length)];

// will return one top polish randomly from the array
const randomElement = TopPolish[Math.floor(Math.random() * TopPolish.length)];
